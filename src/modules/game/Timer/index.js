import React, { Component } from 'react'
import moment from 'moment'

class Timer extends Component {
  render() {
    const { timeToEnd } = this.props
    return (
      <div>
        
      </div>
    )
  }
}

export default Timer
